import { ChangeBooleanPipe } from './change-boolean.pipe';

describe('ChangeBooleanPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeBooleanPipe();
    expect(pipe).toBeTruthy();
  });
});
